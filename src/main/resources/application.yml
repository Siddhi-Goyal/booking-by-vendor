server:
  port: 8080

spring:
  cache:
    type: caffeine
    cache-names: smee-usertype-load
    caffeine:
      spec: maximumSize=500, expireAfterAccess=24h

management:
  endpoints:
    web:
      exposure:
        include: info, health, env
  endpoint:
    health:
      show-details: always

jasypt:
  encryptor:
    algorithm: PBEWithMD5AndDES
    password: ${JASYPT_ENCRYPTOR_PASSWORD:}
